# 字弈 (Ziai) — 设计文档

> 静态网页笔记 + 追小偷跑酷小游戏

---

## 1. 项目概述

### 1.1 产品定位

一款融合 **创意笔记** 与 **跑酷追小偷** 玩法的单页静态网页应用。用户在日常记录笔记的过程中，通过笔记数量的积累解锁游戏能力，在"笔记被偷→追逐小偷"的核心循环中获得乐趣。

### 1.2 技术选型

| 维度 | 选型 | 原因 |
|------|------|------|
| 架构 | 纯静态 SPA | 无后端依赖，可直接用浏览器打开 |
| UI 框架 | 原生 HTML/CSS/JS | 零依赖，体积极小，加载快 |
| 游戏渲染 | Canvas 2D | 跑酷游戏帧率可控，绘制灵活 |
| 数据持久化 | localStorage | 笔记数据 + 游戏进度 + 解锁状态 |
| 字体加载 | CSS `@font-face` + Google Fonts CDN | 按需加载，不阻塞首屏 |

### 1.3 兼容性

- 目标浏览器：Chrome 90+, Firefox 90+, Edge 90+, Safari 15+
- 响应式：支持 Desktop（1280px+）、Tablet（768px~1279px）、Mobile（< 768px）
- 移动端：提供虚拟按键（触摸跳跃/下滑）

---

## 2. 功能模块

### 2.1 笔记系统

#### 2.1.1 笔记 CRUD

```
笔记 = {
  id: string,          // UUID
  title: string,       // 标题
  content: string,     // 正文
  fontFamily: string,  // 字体
  effect: string,      // 特效类型
  createdAt: number,   // 时间戳
  updatedAt: number,
}
```

- **新建**：顶部工具栏点击"+ 新笔记"，弹出编辑面板
- **编辑**：点击笔记卡片进入编辑模式
- **删除**：长按或右键菜单 → 确认删除
- **列表**：卡片网格布局，按更新时间倒序排列

#### 2.1.2 字体系统

**基础字体（12 种）：**

| 类别 | 字体 | CSS font-family |
|------|------|----------------|
| 系统默认 | 默认 | system-ui, sans-serif |
| 宋体 | 宋体 | 'Noto Serif SC', serif |
| 楷体 | 楷体 | 'ZCOOL KaiSerif', cursive |
| 黑体 | 黑体 | 'Noto Sans SC', sans-serif |
| 仿宋 | 仿宋 | 'FangZheng FangSong', serif |
| 圆体 | 圆体 | 'ZCOOL QingKe HuangYou', cursive |
| 草书 | 草书 | 'Ma Shan Zheng', cursive |
| 等宽 | 代码 | 'JetBrains Mono', monospace |
| 可爱 | 娃娃体 | 'ZCOOL KuaiLe', cursive |
| 瘦金 | 瘦金体 | 'Liu Jian Mao Cao', cursive |
| 手写 | 手写体 | 'Long Cang', cursive |
| 像素 | 像素体 | 'Press Start 2P', cursive |

#### 2.1.3 特效字体（CSS 效果叠加）

| 特效 | 实现方式 | 参数 |
|------|---------|------|
| 🌟 霓虹灯 | `text-shadow` 多层发光 | 颜色可调，默认 #0ff |
| 🔥 火焰字 | `background-clip: text` + 渐变动画 | 红-橙-黄渐变 |
| 🕹️ 像素风 | `image-rendering: pixelated` + 像素字体 + 锯齿阴影 | 8px 网格对齐 |
| 🌈 彩虹字 | `background: linear-gradient(…, …)` 动画滚动 | 7 色循环 |
| ❄️ 冰霜字 | `text-shadow` 蓝白辉光 + 半透明渐变 | 冰蓝基调 |
| 📜 复古打字机 | 等宽字体 + `text-shadow` 墨迹晕染 + 纸张纹理背景 | 褐色/米黄 |
| 💀 恐怖字 | 红色 `text-shadow` 抖动 + `skew` 变换 | 随机微偏 |
| 🪞 镜像字 | `transform: scaleY(-1)` + 半透明渐变 | 垂直翻转 |
| ✨ 描边字 | `-webkit-text-stroke` + 渐变填充 | 描边粗细可调 |
| 🎪 3D 立体字 | 多层 `text-shadow` 模拟浮雕 | 8 方向偏移 |

### 2.2 游戏系统

#### 2.2.1 游戏故事

> 小偷从右侧闯入，随机偷走你的一篇笔记，向左逃窜。你操控角色在后方追逐，途中需要躲避各种障碍物。追上小偷即夺回笔记，获得分数奖励。

#### 2.2.2 游戏启动

- **入口**：主界面右上角固定游戏图标按钮
- **触发**：点击后弹出确认框"小偷正在偷走你的《XXX》！是否追击？"
- **被偷笔记**：从现有笔记中随机选取一篇；若笔记数为 0，使用默认占位文字"空白笔记"
- **场景过渡**：页面平滑切换到全屏 Canvas 游戏画面

#### 2.2.3 核心玩法

```
┌──────────────────────────────────────────────────────┐
│  ☁️  ☁️    ☁️      ☁️           ☁️    ☁️  ☁️       │
│                                                      │
│         🏃  ← 玩家           🦹 ← 小偷              │
│       [地面奔跑]         [前方逃窜]                │
│                                                      │
│  ───🌵──────────🪨────🦅────🌵───────────🌵───    │
│  ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓    │
│                                                      │
│  分数: 1280   距离小偷: ████░░░░░ 45m              │
│  笔记: 《静夜思》  生命: ❤️❤️  能力: 🏃💨🦘       │
└──────────────────────────────────────────────────────┘
```

#### 2.2.4 操作方式

| 操作 | 键盘 | 移动端 | 解锁条件 |
|------|------|--------|---------|
| 跳跃 | `Space` / `↑` / `W` | 左半屏点击 | 初始 |
| 下滑 | `↓` / `S` | 右半屏下滑手势 | **10 条笔记** |
| 二段跳 | 空中再次跳跃 | 空中再次点击 | **30 条笔记** |
| 冲刺 | `Shift` / `Ctrl` | 双击右半屏 | **50 条笔记** |
| 暂停 | `Esc` / `P` | 暂停按钮 | 初始 |

#### 2.2.5 障碍物设计

| 障碍物 | 高度 | 躲避方式 | 出现频率 | 速度 |
|--------|------|---------|---------|------|
| 🌵 仙人掌（小） | 低 | 跳跃 | 高 | 基准 |
| 🌵🌵 仙人掌（大） | 高 | 跳跃 | 中 | 基准 |
| 🪨 岩石 | 低 | 跳跃 / 下滑 | 中 | 基准 |
| 🦅 飞鸟（低飞） | 中 | 下滑 | 中低 | 基准 ×1.2 |
| 🦅 飞鸟（高飞） | 高 | 无需躲避 | 中低 | 基准 ×1.2 |
| 🕳️ 裂缝 | 地面 | 跳跃 | 低 | 基准 |
| 🦇 蝙蝠群 | 中高 | 下滑 | 低（后期） | 基准 ×1.5 |
| 🧱 断墙 | 全高 | 下滑 | 极低（后期） | 基准 ×0.8 |

**难度曲线：**
- 每 500m 距离，速度递增 5%
- 每 1000m 距离，障碍密度递增 8%
- 速度上限：基准速度 × 3.0

#### 2.2.6 小偷 AI

```
小偷行为状态机：
  [正常逃跑] ──距离拉开→ [嘲讽] ──距离缩短→ [慌张加速]
      │                      │                    │
      ├─ 70% 概率正常跑      ├─ 播放嘲讽动画      ├─ 加速 15%
      ├─ 20% 概率小幅变速    ├─ 回头看一眼        ├─ 失误概率上升
      └─ 10% 概率跳跃障碍    └─ 可被玩家追上      └─ 约 3s 后回到正常
```

- 小偷初始在玩家前方 **80m** 处
- 追逐距离共 **200m**（玩家追上即胜利）
- 若玩家落后超过 **300m**（即被甩开 300m），游戏失败
- 小偷也会躲避障碍物，但有一定失误率（随难度递增，初始 5% → 最大 25%）

#### 2.2.7 碰撞检测

- 使用 AABB（轴对齐矩形包围盒）碰撞检测
- 玩家和障碍物的碰撞框比视觉小 20%（"仁慈判定"）
- 冲刺期间玩家碰撞框缩小至 30%，且无视所有障碍物碰撞

#### 2.2.8 计分系统

| 行为 | 得分 |
|------|------|
| 每秒存活 | +10 分 |
| 每前进 1m | +5 分 |
| 追上小偷（胜利） | +500 分 |
| 完美躲避（擦过障碍 < 5px） | +50 分 |
| 冲刺中穿越障碍 | +25 分/个 |
| 被偷笔记字数 × 1 | +额外分 |

**排行榜：** localStorage 存储历史最高分 Top 10

### 2.3 进阶系统

#### 2.3.1 解锁总览

```
笔记数量:  0        10       30       50       100
           │────────│────────│────────│────────│
能力:      基础跳跃  解锁下滑  解锁二段跳 解锁冲刺  解锁两条命
           (初始)   ↓ 键    空中再跳  Shift   额外生命
                                (无敌)
```

#### 2.3.2 各能力详细设计

**下滑（10 条笔记解锁）：**
- 持续时间：0.5s
- 冷却时间：0.3s
- 效果：角色高度减半，可通过低矮障碍物（飞鸟、蝙蝠群）
- 不能过全高障碍（断墙 — 需冲刺撞破）

**二段跳（30 条笔记解锁）：**
- 第一次跳跃：高度 120px
- 第二次跳跃（空中）：高度 80px（约为第一跳的 67%）
- 限制：必须在第一次跳跃下降阶段触发，上升阶段不可
- 落地后重置

**冲刺（50 条笔记解锁）：**
- 持续时间：0.4s
- 冷却时间：3s
- 效果：
  - 速度 × 3
  - **无敌状态**（无视所有碰撞）
  - 撞碎路径上的小型障碍物（仙人掌、岩石）
  - 画面出现速度线特效
- 视觉反馈：角色发光 + 残影拖尾 + 屏幕边缘速度线

**额外生命（100 条笔记解锁）：**
- 开局携带 2 条命（而非 1 条）
- 碰撞后：
  - 失去一条命，角色闪烁 2s（无敌时间）
  - 保留当前位置继续追赶
  - 生命耗尽 → 游戏结束
- 视觉：左上角显示 ❤️❤️ 而非 ❤️
- 无法在游戏中获取额外生命

#### 2.3.3 解锁提示

- 笔记数达到阈值时，主界面弹出庆祝动画：
  - 粒子爆炸效果
  - "🎉 恭喜！已解锁【下滑】能力！"
  - 游戏入口图标新增红点/角标提示
- 首次解锁该能力时，游戏中加入简短的教程提示（仅显示一次）

---

## 3. 界面设计

### 3.1 主界面（笔记模式）

```
┌─────────────────────────────────────────────────────────┐
│  📝 字弈                    [+] 新建笔记    🎮 [追小偷]  │
│─────────────────────────────────────────────────────────│
│  已记录: 32 条  |  已解锁: 跳跃 下滑 二段跳             │
│─────────────────────────────────────────────────────────│
│                                                         │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐  │
│  │ 静夜思   │ │ 春晓     │ │ 登鹳雀楼 │ │ 江雪     │  │
│  │ 宋体·霓虹│ │ 楷体·彩虹│ │ 像素·像素│ │ 手写·打字│  │
│  │ 2分钟前  │ │ 1小时前  │ │ 3小时前  │ │ 昨天     │  │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘  │
│                                                         │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐               │
│  │ ...      │ │ ...      │ │ ...      │               │
│  └──────────┘ └──────────┘ └──────────┘               │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 3.2 笔记编辑面板

```
┌───────────────────────────────────────────────┐
│  [← 返回]         编辑笔记          [保存] [删]│
│───────────────────────────────────────────────│
│  标题: [________________________]             │
│                                               │
│  字体: [宋体 ▾]  特效: [霓虹灯 ▾]            │
│                                               │
│  ┌─────────────────────────────────────────┐  │
│  │                                         │  │
│  │  (实时预览编辑区域)                       │  │
│  │                                         │  │
│  │  床前明月光，疑是地上霜。                  │  │
│  │  举头望明月，低头思故乡。                  │  │
│  │                                         │  │
│  │                                         │  │
│  └─────────────────────────────────────────┘  │
│                                               │
└───────────────────────────────────────────────┘
```

### 3.3 游戏画面

```
┌─────────────────────────────────────────────────────────┐
│  ❤️❤️  ████████░░░░░░░░  45m / 200m    🏆 3520         │
│                                                         │
│    ☁️        ☁️              ☁️         ☁️              │
│                                                         │
│                                                         │
│              🦹 (前方 45m)                               │
│        🏃                                              │
│                                                         │
│  ─────────────🌵────🪨────────🦅───────────🌵──────    │
│  ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓    │
│                                                         │
│  [暂停 ⏸]                    [⬆跳] [⬇滑] [💨冲刺]      │
└─────────────────────────────────────────────────────────┘
```

### 3.4 色系方案

| 区域 | 背景色 | 前景色 | 强调色 |
|------|--------|--------|--------|
| 主界面 | `#1a1a2e` 深蓝黑 | `#e0e0e0` 浅灰白 | `#e94560` 珊瑚红 |
| 编辑面板 | `#16213e` 暗蓝 | `#f5f5f5` 近白 | `#0f3460` 深蓝 |
| 游戏天空 | `#87CEEB` → `#1a1a2e` 渐变 | — | — |
| 游戏地面 | `#4a3728` 棕色 | `#8b7355` 浅棕 | — |
| 解锁提示 | `#ffd700` 金色 | `#1a1a2e` | `#ff6b6b` |

---

## 4. 技术架构

### 4.1 文件结构

```
ziai/
├── index.html          # 入口文件，单页 HTML
├── css/
│   ├── reset.css       # CSS Reset
│   ├── main.css        # 主界面样式
│   ├── editor.css      # 编辑器样式
│   ├── game.css        # 游戏界面样式
│   ├── fonts.css       # 字体定义与加载
│   └── effects.css     # 特效字体 CSS
├── js/
│   ├── app.js          # 应用入口，模块初始化
│   ├── storage.js      # 多层存储封装（读写/校验/备份/恢复/导出/导入）
│   ├── notes.js        # 笔记 CRUD 逻辑
│   ├── editor.js       # 编辑器交互逻辑
│   ├── progression.js  # 解锁系统逻辑
│   ├── game/
│   │   ├── core.js     # 游戏主循环、状态管理
│   │   ├── player.js   # 玩家角色（跳跃/下滑/冲刺/碰撞）
│   │   ├── thief.js    # 小偷 AI
│   │   ├── obstacles.js # 障碍物生成与管理
│   │   ├── background.js # 视差滚动背景
│   │   ├── effects.js  # 粒子特效、残影、速度线
│   │   ├── ui.js       # 游戏内 HUD 绘制
│   │   └── input.js    # 键盘/触摸输入处理
│   └── utils.js        # 通用工具函数
├── assets/
│   ├── sprites/        # 角色精灵图（像素风）
│   │   ├── player.png
│   │   ├── thief.png
│   │   └── obstacles.png
│   ├── sounds/         # 音效（可选，使用 Web Audio API 合成）
│   └── icons/          # UI 图标
└── DESIGN.md           # 本设计文档
```

### 4.2 游戏主循环

```javascript
// 伪代码
class Game {
  constructor() {
    this.state = 'ready';     // ready | playing | paused | won | lost
    this.speed = 6;           // 基准速度 (px/frame)
    this.distance = 200;      // 追逐总距离
    this.playerPosition = 0;
    this.thiefPosition = 80;
    this.score = 0;
    this.lives = 1;
    this.canSlide = false;
    this.canDoubleJump = false;
    this.canDash = false;
    this.extraLife = false;
  }

  loop(timestamp) {
    this.update(timestamp);
    this.render();
    if (this.state === 'playing') {
      requestAnimationFrame(this.loop.bind(this));
    }
  }

  update(dt) {
    this.updateBackground(dt);
    this.updatePlayer(dt);
    this.updateThief(dt);
    this.updateObstacles(dt);
    this.checkCollisions();
    this.updateScore(dt);
  }

  render() {
    this.ctx.clearRect(0, 0, this.width, this.height);
    this.renderBackground();
    this.renderObstacles();
    this.renderThief();
    this.renderPlayer();
    this.renderEffects();
    this.renderHUD();
  }
}
```

### 4.3 数据流

```
                         ┌──────────────────────┐
                         │   用户操作 (CRUD)     │
                         └──────────┬───────────┘
                                    │
                                    ▼
                         ┌──────────────────────┐
                         │   storage.js          │
                         │   ┌────────────────┐  │
                         │   │ 写入队列 (Promise)│  │
                         │   │ 校验和计算       │  │
                         │   │ sessionStorage  │  │
                         │   │ 临时副本         │  │
                         │   └───────┬────────┘  │
                         │           │           │
                         │    校验通过？         │
                         │     ├─ 是 → 写入     │
                         │     └─ 否 → 回滚     │
                         └──────────┬───────────┘
                                    │
            ┌───────────────────────┼───────────────────────┐
            ▼                       ▼                       ▼
   ┌────────────────┐    ┌──────────────────┐    ┌──────────────────┐
   │ localStorage    │    │ 自动备份 (多层)    │    │ 文件导出          │
   │                │    │                  │    │                  │
   │ ziai_notes     │    │ snapshot (实时)   │    │ .ziai-backup.json │
   │ ziai_settings  │    │ hourly  × 24     │    │ (用户手动/自动)    │
   │ ziai_scores    │    │ daily   × 30     │    │                  │
   └───────┬────────┘    │ weekly  × 12     │    └────────┬─────────┘
           │             └────────┬─────────┘             │
           │                      │                       │
           └──────────────────────┼───────────────────────┘
                                  │
                                  ▼
                    ┌─────────────────────────┐
                    │   启动时健康检查 (第4层)   │
                    │   损坏 → 逐级回退恢复      │
                    └─────────────────────────┘
                                  │
              ┌───────────────────┼───────────────────┐
              ▼                   ▼                   ▼
        ┌─────────┐       ┌──────────────┐    ┌──────────┐
        │ NoteList │       │ Progression  │    │ Game Core│
        │ Editor   │       │ System       │    │          │
        └─────────┘       └──────────────┘    └──────────┘
```

### 4.4 localStorage 数据模型

```javascript
// Key: "ziai_notes"
[
  {
    id: "uuid-1",
    title: "静夜思",
    content: "床前明月光，疑是地上霜。\n举头望明月，低头思故乡。",
    fontFamily: "noto-serif-sc",
    effect: "neon",
    createdAt: 1719561600000,
    updatedAt: 1719561600000,
  },
  // ...
]

// Key: "ziai_settings"
{
  unlockedAbilities: ["jump", "slide", "doubleJump", "dash", "extraLife"],
  tutorialsSeen: { slide: true, doubleJump: false },
  theme: "dark",
  soundEnabled: true,
}

// Key: "ziai_scores"
[
  { score: 12800, date: 1719561600000, noteTitle: "静夜思" },
  // ... Top 10
]
```

### 4.5 数据防丢失策略（多层防护）

localStorage 存在天然风险：浏览器清缓存会清除、隐私模式受限、跨设备无法同步、存储空间仅 5~10MB、写入时崩溃可能损坏数据。以下采用 **5 层防护** 逐级兜底。

---

#### 第 0 层：写入安全（防损坏）

```
写入流程：
  1. 序列化数据 → JSON 字符串
  2. 先写入 sessionStorage（临时副本，防止写入中断丢失旧数据）
  3. 写入 localStorage
  4. 校验：回读 localStorage 并对比 MD5/CRC32 校验和
  5. 校验通过 → 清除 sessionStorage 临时副本
  6. 校验失败 → 从 sessionStorage 回滚到 localStorage，提示用户
```

- **原子写入**：每次保存为完整快照，不做增量更新（单个笔记编辑也走全量写入，确保一致性）
- **写入锁**：同一时刻只允许一个写操作进行（Promise 队列）
- **storage 事件监听**：若同域其他标签页修改了数据，自动同步到当前页

#### 第 1 层：自动本地备份（防误删/崩溃）

```
备份策略：
  ┌─────────────────────────────────────────────────┐
  │ 备份层级      │ 触发时机            │ 保留份数  │
  ├─────────────────────────────────────────────────┤
  │ 实时快照      │ 每次保存后          │ 1 份      │
  │ 小时备份      │ 每小时 (有变更时)    │ 24 份     │
  │ 日备份        │ 每天首次打开页面     │ 30 份     │
  │ 周备份        │ 每周一              │ 12 份     │
  └─────────────────────────────────────────────────┘
```

- 所有备份存储在 localStorage 独立 key 下：
  - `ziai_backup_snapshot` — 上次保存的即时快照
  - `ziai_backup_hourly_YYYYMMDDHH` — 24 个
  - `ziai_backup_daily_YYYYMMDD` — 30 个
  - `ziai_backup_weekly_YYYYWW` — 12 个
- 超过保留份数的旧备份自动清理
- 备份含元数据：`{ version, timestamp, checksum, noteCount, data }`

#### 第 2 层：导出到文件（防 localStorage 清空）

| 导出方式 | 触发方式 | 说明 |
|---------|---------|------|
| **手动导出** | 设置面板 → "导出全部数据" | 下载 `.ziai-backup.json` 文件 |
| **定期提醒** | 每 30 天弹一次 toast | "已帮你守护笔记 X 天，建议导出备份" |
| **快捷导出** | 笔记数 ≥ 20 时，主界面底部常驻导出按钮 | 降低操作门槛 |
| **自动下载** | 笔记数达 50/100 时自动触发下载 | 里程碑自动备份 |

- 导出格式：JSON（含完整笔记数据、设置、排行榜）
- 文件命名：`ziai-backup-2026-06-28.json`
- MIME type 设为 `application/json`，触发浏览器下载

#### 第 3 层：导入恢复（防意外）

```
恢复入口：设置面板 → "导入数据"
恢复流程：
  1. 用户选择 .ziai-backup.json 文件
  2. 读取并校验格式 + 校验和
  3. 预览：显示备份中的笔记数、日期
  4. 合并策略选择：
     ├─ [推荐] 智能合并：同 ID 笔记保留最新版本，新笔记追加
     ├─ 完全覆盖：用备份替换当前全部数据
     └─ 仅导入缺失：只导入本地不存在的笔记
  5. 合并前自动生成一次"恢复前快照"（防止误操作）
  6. 确认 → 写入 → 刷新界面
```

#### 第 4 层：启动时数据健康检查

```
页面加载时执行（storage.js init）：
  1. 读取 localStorage 主数据
  2. 校验 JSON 格式 → 损坏 → 尝试从 snapshot 恢复
  3. snapshot 也坏 → 尝试从最新小时备份恢复
  4. 小时备份也坏 → 尝试日备份 → 周备份
  5. 全部失败 → 提示用户导入文件备份
  6. 校验通过 → 检查是否触发日/周备份
  7. 如果当前数据条目数 < 最近备份条目数 →
     弹窗提示："检测到可能发生了数据丢失，是否从 X 天前的备份恢复？"
```

#### 各层防护覆盖的风险矩阵

| 风险场景 | 第 0 层 | 第 1 层 | 第 2 层 | 第 3 层 | 第 4 层 |
|---------|:---:|:---:|:---:|:---:|:---:|
| 写入时崩溃/断电 | ✅ | — | — | — | ✅ |
| 代码 bug 导致写坏数据 | ✅ | ✅ | — | — | ✅ |
| 用户误删笔记 | — | ✅ | — | — | — |
| 浏览器清缓存 | — | — | ✅ | ✅ | — |
| 换电脑/换浏览器 | — | — | ✅ | ✅ | — |
| 隐私模式窗口关闭 | — | — | ✅ | — | — |
| localStorage 被其他扩展篡改 | ✅ | ✅ | — | — | ✅ |
| 硬盘故障 | — | — | ✅ | — | — |

#### 存储空间监控

```javascript
// storage.js
function checkQuota() {
  const used = JSON.stringify(localStorage).length;
  const limit = 5 * 1024 * 1024; // 5MB 保守估计
  const usage = used / limit;

  if (usage > 0.9) {
    showWarning('存储空间即将用尽，请导出备份并清理旧备份');
  }
  if (usage > 0.75) {
    pruneOldBackups(); // 自动清理超过保留期的备份
  }
  return usage;
}
```

---

## 5. 美术风格

### 5.1 整体方向

**像素艺术 (Pixel Art)** — 致敬经典 8-bit/16-bit 游戏，同时与笔记的"像素特效字体"形成呼应。

- 角色：16×16 或 32×32 像素精灵
- 场景：像素风地面、仙人掌、云朵
- UI：像素字体（Press Start 2P）+ 粗像素边框
- 配色：高饱和度 + 强对比度

### 5.2 角色设计

**玩家角色：** 奔跑的书生/诗人形象
- 站立：头戴方巾，手持毛笔
- 奔跑：身体前倾，笔在身后
- 跳跃：身体收缩，笔上举
- 下滑：身体趴低滑行
- 冲刺：身体水平，笔在前，身后残影
- 死亡：绊倒，笔飞出去

**小偷角色：** 蒙面忍者/飞贼形象
- 奔跑：怀中抱着偷来的卷轴（笔记）
- 嘲讽：回头做鬼脸
- 慌张：速度加快，头发竖起

### 5.3 动画帧率

- 角色动画：8 FPS（8 帧精灵图循环）
- 游戏逻辑：60 FPS（requestAnimationFrame）
- UI 过渡：CSS transition 300ms

---

## 6. 音效设计（可选）

| 音效 | 触发时机 | 类型 |
|------|---------|------|
| 跳跃 | 玩家跳跃 | 短促上升音 (Web Audio 方波) |
| 二段跳 | 二段跳触发 | 同上 + 音调高八度 |
| 下滑 | 下滑开始 | 低沉的"嗖"声 (噪声滤波) |
| 冲刺 | 冲刺激活 | 轰鸣声 + 心跳加速 |
| 碰撞 | 撞到障碍物 | 低音爆破 |
| 胜利 | 追上小偷 | 胜利号角 (3 音符上行) |
| 失败 | 被甩开 | 悲伤下行旋律 |
| 解锁 | 解锁新能力 | 叮咚 + 粒子音 |
| 背景音乐 | 游戏中循环 | 8-bit Chiptune 风格（可选） |

> 音效使用 Web Audio API 实时合成，无需音频文件。

---

## 7. 开发路线图

### Phase 1 — 基础框架（MVP）

- [ ] 项目骨架搭建：HTML + CSS 文件结构
- [ ] **多层存储系统**：storage.js 含校验和 + sessionStorage 临时副本 + 写入队列
- [ ] 笔记 CRUD（新建/编辑/删除，localStorage 持久化）
- [ ] **自动备份**：实时快照 + 小时/日/周备份 + 启动健康检查
- [ ] **导出/导入**：JSON 文件导出 + 智能合并恢复
- [ ] 基础字体选择器（6 种字体）
- [ ] 基础特效（霓虹灯、彩虹字、像素风）
- [ ] 主界面布局（笔记卡片网格）

### Phase 2 — 游戏核心

- [ ] Canvas 游戏框架搭建
- [ ] 游戏主循环 + 渲染管线
- [ ] 玩家角色（基础跳跃 + 碰撞检测）
- [ ] 小偷 AI（基本行为状态机）
- [ ] 障碍物系统（仙人掌、岩石、飞鸟）
- [ ] 游戏 HUD（分数、距离、生命）
- [ ] 游戏结束/胜利判定

### Phase 3 — 进阶系统

- [ ] 解锁系统（与笔记数联动）
- [ ] 下滑能力实现
- [ ] 二段跳能力实现
- [ ] 冲刺能力 + 无敌实现
- [ ] 额外生命实现
- [ ] 解锁动画 + 教程提示

### Phase 4 — 打磨优化

- [ ] 移动端适配 + 触摸操作
- [ ] 粒子特效（灰尘、碎屑、速度线）
- [ ] 音效系统（Web Audio API）
- [ ] 排行榜
- [ ] 性能优化（离屏 Canvas、对象池）
- [ ] 更多特效字体
- [ ] 暗色/亮色主题切换

### Phase 5 — 扩展（可选）

- [ ] 成就系统
- [ ] 多个小偷皮肤
- [ ] 每日挑战模式
- [ ] 笔记导出（JSON / Markdown）
- [ ] PWA 支持（离线可用）

---

## 8. 关键交互流程

### 8.1 完整游戏流程

```
主界面（笔记列表）
  │
  ├─ 点击"追小偷"按钮
  │   │
  │   ├─ 笔记数 == 0? → 提示"还没有笔记，先去写一篇吧！" → 返回
  │   └─ 笔记数 > 0 →
  │       随机选取一篇笔记
  │       显示确认框："🦹 小偷正在偷走你的《XXX》！是否追击？"
  │         │
  │         ├─ [取消] → 返回主界面
  │         └─ [追击！] → 场景切换到游戏画面
  │              │
  │              游戏开始
  │              │
  │              ├─ 追上小偷 → 🎉 胜利画面
  │              │   "夺回了《XXX》！+1280 分"
  │              │   [再来一局] [返回主页]
  │              │
  │              └─ 被甩开 / 生命耗尽 → 💀 失败画面
  │                  "小偷带着《XXX》逃走了..."
  │                  "不用担心，笔记还在你的收藏里！"
  │                  [再来一局] [返回主页]
  │
  └─ 新建/编辑笔记
       │
       保存笔记
       │
       ├─ 笔记数达到阈值? → 弹出解锁动画 → 更新 settings
       └─ 正常保存
```

### 8.2 状态流转

```
                    ┌─────────┐
                    │  IDLE   │ (主界面)
                    └────┬────┘
                         │ 点击"追小偷"
                         ▼
                    ┌─────────┐
                    │ CONFIRM │ (确认弹窗)
                    └────┬────┘
                    ┌────┴────┐
                    ▼         ▼
               ┌────────┐  ┌─────────┐
               │ READY  │  │  IDLE   │ (取消)
               └───┬────┘  └─────────┘
                   │ 自动开始
                   ▼
              ┌─────────┐     ┌──────────┐
              │ PLAYING │────▶│  PAUSED  │
              └────┬────┘ Esc └────┬─────┘
                   │               │ 继续
         ┌─────────┴─────────┐     │
         ▼                   ▼     │
    ┌────────┐          ┌────────┐  │
    │  WON   │          │  LOST  │  │
    └────┬───┘          └────┬───┘  │
         │                   │      │
         └─────────┬─────────┘      │
                   ▼                │
             ┌──────────┐           │
             │  RESULT  │◄──────────┘
             └──────────┘
```

---

## 9. 附录

### 9.1 精灵图规格

| 精灵 | 尺寸 | 帧数 | 格式 |
|------|------|------|------|
| 玩家-奔跑 | 32×32 | 6 帧 | PNG (透明) |
| 玩家-跳跃 | 32×32 | 2 帧 | PNG |
| 玩家-下滑 | 32×16 | 2 帧 | PNG |
| 玩家-冲刺 | 32×32 | 4 帧 | PNG |
| 玩家-死亡 | 32×32 | 4 帧 | PNG |
| 小偷-奔跑 | 32×32 | 6 帧 | PNG |
| 小偷-嘲讽 | 32×32 | 4 帧 | PNG |
| 小偷-慌张 | 32×32 | 6 帧 | PNG |
| 仙人掌 | 16×32 | 1 帧 | PNG |
| 岩石 | 24×16 | 1 帧 | PNG |
| 飞鸟 | 16×16 | 2 帧 | PNG |
| 地面瓦片 | 32×32 | 1 帧 | PNG |

### 9.2 Canvas 画布规格

- 游戏分辨率：960 × 540（16:9 逻辑分辨率）
- 实际渲染：等比缩放至容器宽度
- 最大宽度：1280px
- 元素以逻辑坐标绘制，`ctx.scale()` 适配实际尺寸

### 9.3 性能指标

| 指标 | 目标值 |
|------|--------|
| 首屏加载 | < 1.5s |
| 游戏帧率 | 稳定 60 FPS |
| 内存占用 | < 50MB |
| localStorage 占用 | < 5MB |
| 离线可用 | ✅ (无网络请求依赖) |

---

> **文档版本：** v1.0  
> **创建日期：** 2026-06-28  
> **状态：** 待评审
